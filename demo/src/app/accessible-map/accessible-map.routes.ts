import { AccessibleMapComponent } from './accessible-map.component';

export const AccessibleMapRoutes = [
  {
    path: '/accessible-map',
    component: AccessibleMapComponent,
    index: true
  }
];
